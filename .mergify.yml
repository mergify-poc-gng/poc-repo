queue_rules:
  - name: default
    queue_conditions:
      - "label=ready-to-queue"
    merge_conditions:
      - "check-success=gate-never-exists"
pull_request_rules:
  - name: queue-on-label
    conditions:
      - "label=ready-to-queue"
    actions:
      queue:
        name: default