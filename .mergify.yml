pull_request_rules:
  - name: squash merge ta
    conditions:
      - base = ta-main
    actions:
      merge:
        method: squash
        commit_message_template: |
          {{ title }}

          {{ body }}
