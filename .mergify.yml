pull_request_rules:
  - name: clear blocks on label
    conditions:
      - "label = clearblock"
    actions:
      dismiss_reviews:
        approved: false
        changes_requested: true
        message: dismissed by rule
