pull_request_rules:
  - name: imp-comment-fire
    conditions:
      - "label = impfire"
    actions:
      comment:
        message: "IMP-PROBE actor={{ author }} (Mergify bot_account impersonation test)"
        bot_account: "{{ author }}"
