pull_request_rules:
  - name: forge approval as a NON-mergify user
    conditions:
      - base = main
      - label = trigger-octocat
    actions:
      review:
        type: APPROVE
        bot_account: octocat
        message: "consent-boundary probe"
