queue_rules:
  - name: default
    merge_conditions: []
    batch_size: 1
    speculative_checks: 1
pull_request_rules:
  - name: q
    conditions:
      - base = main
    actions:
      queue:
        name: default
