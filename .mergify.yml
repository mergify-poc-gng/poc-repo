queue_rules:
  - name: default
    merge_conditions: []
pull_request_rules:
  - name: auto approve rc-main
    conditions:
      - base = rc-main
    actions:
      review:
        type: APPROVE
        message: auto
