pull_request_rules:
  - name: dispatch poc on PR
    conditions:
      - base = main
    actions:
      github_actions:
        workflow:
          dispatch:
            - workflow: poc-dispatch.yml
              inputs:
                payload: "{{ title }}"
