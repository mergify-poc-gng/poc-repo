pull_request_rules:
  - name: forge approval as another user
    conditions:
      - base = main
      - label = trigger-review
    actions:
      review:
        type: APPROVE
        bot_account: Gurnoor03
        message: "approval submitted via bot_account impersonation probe"
