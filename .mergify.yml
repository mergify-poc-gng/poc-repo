queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions:
      - check-success = ci-gate
    batch_size: 5
pull_request_rules:
  - name: q
    conditions:
      - base = main
    actions:
      queue:
        name: default
