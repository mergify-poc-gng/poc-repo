queue_rules:
  - name: default
    queue_conditions:
      - "label=ready-to-queue"
    merge_conditions:
      - "#approved-reviews-by>=1"
pull_request_rules:
  - name: queue-on-label
    conditions:
      - "label=ready-to-queue"
    actions:
      queue:
        name: default