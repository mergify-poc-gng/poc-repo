queue_rules:
  - name: default
    batch_size: 1
    merge_conditions:
      - check-success = ci
pull_request_rules:
  - name: q
    conditions:
      - check-success = ci
      - base = main
    actions:
      queue:
